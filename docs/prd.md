# وثيقة المتطلبات

## 1. نظرة عامة على التطبيق

### 1.1 اسم التطبيق
الوسيلة الذكية

### 1.2 وصف التطبيق
منصة تعليمية ذكية لإدارة المحتوى الدراسي وتوزيع الدروس على الطلاب من خلال نظام أكواد آمن، مع دعم الذكاء الاصطناعي لتحليل المحتوى وتوليد الأسئلة والملخصات والاختبارات التفاعلية. التطبيق يعمل بدون اتصال بالإنترنت بشكل كامل بعد الدخول الأولي، مع تحديثات ذكية في الخلفية.

### 1.3 نطاق التعديل
تحويل نظام التفعيل من التخزين المحلي إلى نظام حسابات على السيرفر لحل مشاكل:
- فقدان التفعيل عند حذف التطبيق أو مسح الكاش
- عدم قبول كلمة المرور بعد إعادة التثبيت
- الحاجة لإعادة التفعيل عند تغيير المتصفح أو الجهاز
- عدم ظهور الملاحظات والأسئلة المحفوظة بعد إعادة التثبيت
- مشاكل التحديث التلقائي والإحصائيات
- تحسين تجربة المستخدم والواجهة

---

## 2. نظام الحسابات والتفعيل الجديد

### 2.1 المفاهيم الأساسية

#### 2.1.1 كود التفعيل (6 خانات)
- يُستخدم مرة واحدة فقط لإنشاء حساب الطالب في السيرفر
- بعد الاستخدام الأول، لا يمكن استخدامه مرة أخرى
- يُخزن في جدول access_codes في Supabase

#### 2.1.2 حساب الطالب
- سجل دائم في السيرفر (جدول student_accounts في Supabase)
- يحتوي على:
  + معرف فريد للطالب
  + كلمة المرور المشفرة
  + الكود المستخدم للتفعيل
  + الصفوف المفعّلة
  + تاريخ آخر دخول
  + حالة التثبيت
  + حالة عرض التعليمات
  + الملاحظات المحفوظة
  + الأسئلة المحفوظة
  + بيانات الإحصائيات

#### 2.1.3 كلمة المرور
- تُنشأ تلقائياً عند أول تفعيل بالكود 6 خانات
- تُستخدم للدخول الدائم من أي جهاز أو متصفح
- يمكن تغييرها من إعدادات الطالب
- مشفرة في السيرفر

#### 2.1.4 كود الصف (7 خانات)
- يُستخدم لتفعيل صف دراسي محدد
- يُضاف إلى حساب الطالب في السيرفر
- يمكن تفعيل أكثر من كود صف

### 2.2 صفحة الدخول الرئيسية

#### 2.2.1 عناصر الصفحة
- شعار التطبيق واسمه في الأعلى
- حقل إدخال الكود أو كلمة المرور
- زر دخول
- نص توضيحي: «أدخل كود التفعيل (6 خانات) للمرة الأولى، أو كلمة المرور للدخول»
- عبارة: © 2026 الوسيلة الذكية | جميع العلامات التجارية محفوظة
- رابط دخول المدير أسفل العبارة السابقة مباشرة

#### 2.2.2 آلية الدخول

##### التفعيل الأول (كود 6 خانات)
1. الطالب يدخل الكود 6 خانات
2. النظام يتحقق من الكود في السيرفر
3. إذا كان الكود صالحاً وغير مستخدم:
   - إنشاء حساب جديد في جدول student_accounts
   - توليد كلمة مرور تلقائية
   - عرض كلمة المرور للطالب مع رسالة: «احفظ كلمة المرور هذه للدخول في المرات القادمة»
   - تمييز الكود كمستخدم في السيرفر
   - حفظ معرف الحساب في localStorage للدخول السريع
4. إذا كان الكود مستخدماً مسبقاً: عرض رسالة خطأ

##### الدخول بكلمة المرور
1. الطالب يدخل كلمة المرور
2. النظام يتحقق من كلمة المرور في السيرفر
3. إذا كانت صحيحة:
   - تحميل بيانات الحساب من السيرفر
   - حفظ معرف الحساب في localStorage
   - تحديث تاريخ آخر دخول في السيرفر
   - الانتقال للصفحة الرئيسية
4. إذا كانت خاطئة: عرض رسالة خطأ

##### الدخول التلقائي
- إذا وُجد معرف حساب في localStorage:
  + التحقق من صلاحية الجلسة في السيرفر (عند وجود إنترنت)
  + الدخول مباشرة للصفحة الرئيسية
  + تحميل البيانات من الكاش المحلي فوراً
  + تحديث البيانات من السيرفر في الخلفية

### 2.3 صفحة تفعيل كود صف جديد

#### 2.3.1 الوصول للصفحة
- متاحة من قائمة الطالب أو الإعدادات
- تتطلب دخول الطالب

#### 2.3.2 وظائف الصفحة
- حقل لإدخال كود صف جديد (7 خانات)
- زر تفعيل الكود
- عرض قائمة الصفوف المفعّلة حالياً
- التحقق من الكود في السيرفر
- إضافة الصف لحساب الطالب في السيرفر
- تحديث القائمة المحلية

### 2.4 إعادة تعيين الكود من المدير

#### 2.4.1 آلية الإعادة
- المدير يمكنه إعادة تعيين كود التفعيل من لوحة التحكم
- عند إعادة التعيين:
  + حذف حساب الطالب المرتبط بالكود من السيرفر
  + تمييز الكود كغير مستخدم

#### 2.4.2 تأثير الإعادة على التطبيق
- عند فتح التطبيق ووجود إنترنت:
  + التحقق من صلاحية الحساب في السيرفر
  + إذا تم حذف الحساب:
    * مسح جميع البيانات المحلية
    * تسجيل الخروج تلقائياً
    * العودة لصفحة الدخول
    * عرض رسالة: «تم إعادة تعيين الكود، يرجى التفعيل من جديد»

### 2.5 ترحيل المستخدمين القدامى

#### 2.5.1 آلية الترحيل
- عند أول فتح بعد التحديث:
  + قراءة الكود وكلمة المرور من localStorage القديم
  + التحقق من وجود إنترنت
  + إذا وُجد إنترنت:
    * إنشاء حساب في السيرفر بنفس الكود وكلمة المرور
    * نقل جميع البيانات المحلية للسيرفر:
      - الصفوف المفعّلة
      - الملاحظات المحفوظة
      - الأسئلة المحفوظة
      - بيانات الإحصائيات
    * حذف البيانات القديمة من localStorage
    * حفظ معرف الحساب الجديد
  + إذا لم يوجد إنترنت:
    * الاستمرار بالبيانات المحلية
    * محاولة الترحيل عند توفر الإنترنت

#### 2.5.2 معالجة الأخطاء
- إذا فشل الترحيل:
  + الاحتفاظ بالبيانات المحلية
  + إعادة المحاولة في الفتح التالي
  + عدم عرض أي رسالة خطأ للمستخدم

---

## 3. هيكل الصفحات والوظائف

### 3.1 صفحة قائمة الدروس

#### 3.1.1 آلية العرض
- عرض البيانات المخزنة في الجهاز فوراً عند فتح القائمة
- جلب البيانات الجديدة من السيرفر في الخلفية (عند وجود إنترنت)
- تحديث القائمة تلقائياً عند وصول البيانات الجديدة
- حذف الدروس المحذوفة وإضافة الجديدة تلقائياً

#### 3.1.2 عناصر الواجهة
- أيقونة تحديث يدوي في أعلى القائمة
- نقطة خضراء صغيرة تظهر لمدة ثانيتين عند التحديث في الخلفية
- عرض الدروس مع الصور المصغرة
- حجم العرض موحد دائماً (لا يتغير بناءً على حالة الاتصال)

#### 3.1.3 التحديث اليدوي
- الضغط على أيقونة التحديث:
  + جلب البيانات الجديدة من السيرفر فوراً (عند وجود إنترنت)
  + عرض مؤشر التحديث الصامت (النقطة الخضراء)
  + تحديث القائمة تلقائياً
  + إذا لم يوجد إنترنت: عرض رسالة مختصرة «لا يوجد اتصال» دون منع استخدام التطبيق

### 3.2 صفحة القائمة الرئيسية (Dashboard)

#### 3.2.1 عناصر الصفحة
- أيقونة تحديث يدوي في أعلى الصفحة
- عرض آخر تاريخ دخول
- عرض الإحصائيات
- الوصول السريع للدروس والاختبارات

#### 3.2.2 آخر تاريخ دخول
- يُحفظ في حساب الطالب في السيرفر
- يُحدّث عند كل دخول (عند وجود إنترنت)
- يُعرض من الكاش المحلي عند عدم وجود إنترنت
- الصيغة: «آخر دخول: 2026-08-01 الساعة 18:37»

#### 3.2.3 التحديث اليدوي
- الضغط على أيقونة التحديث:
  + جلب المحتوى الجديد من السيرفر (عند وجود إنترنت)
  + تحديث الإحصائيات
  + تحديث قائمة الدروس
  + عرض مؤشر التحديث الصامت

### 3.3 صفحة الاختبارات (Quizzes)

#### 3.3.1 آلية العرض
- عرض الاختبارات من الكاش المحلي فوراً
- تحديث من السيرفر في الخلفية (عند وجود إنترنت)
- حجم العرض موحد دائماً

#### 3.3.2 حفظ النتائج
- حفظ نتائج الاختبارات في حساب الطالب في السيرفر
- المزامنة التلقائية عند توفر الإنترنت
- الاحتفاظ بالنتائج محلياً حتى المزامنة

### 3.4 صفحة الملاحظات (Notes)

#### 3.4.1 حفظ الملاحظات
- حفظ الملاحظات في حساب الطالب في السيرفر
- المزامنة التلقائية عند توفر الإنترنت
- الاحتفاظ بالملاحظات محلياً حتى المزامنة

#### 3.4.2 عرض الملاحظات
- عرض الملاحظات من الكاش المحلي فوراً
- تحديث من السيرفر في الخلفية (عند وجود إنترنت)
- ظهور الملاحظات بعد إعادة التثبيت (من حساب السيرفر)

### 3.5 صفحة الأسئلة المحفوظة

#### 3.5.1 حفظ الأسئلة
- حفظ الأسئلة في حساب الطالب في السيرفر
- المزامنة التلقائية عند توفر الإنترنت
- الاحتفاظ بالأسئلة محلياً حتى المزامنة

#### 3.5.2 عرض الأسئلة
- عرض الأسئلة من الكاش المحلي فوراً
- تحديث من السيرفر في الخلفية (عند وجود إنترنت)
- ظهور الأسئلة بعد إعادة التثبيت (من حساب السيرفر)

### 3.6 صفحة الإحصائيات (Statistics)

#### 3.6.1 مصدر البيانات
- جميع بيانات الإحصائيات تُجلب من حساب الطالب في السيرفر
- تشمل:
  + عدد الدروس المكتملة
  + عدد الاختبارات المنجزة
  + متوسط الدرجات
  + الترتيب بين الطلاب
  + وقت الدراسة الإجمالي

#### 3.6.2 آلية العرض
- عرض الإحصائيات من الكاش المحلي فوراً
- تحديث من السيرفر في الخلفية (عند وجود إنترنت)
- عدم عرض رسالة الكود
- حجم العرض موحد دائماً

#### 3.6.3 الترتيب
- حساب الترتيب في السيرفر بناءً على جميع الطلاب
- تحديث الترتيب عند كل مزامنة
- عرض الترتيب من الكاش عند عدم وجود إنترنت

### 3.7 رسالة التثبيت

#### 3.7.1 شروط الظهور
- تظهر مرة واحدة فقط بعد أول تثبيت للتطبيق
- حفظ حالة الظهور في حساب الطالب في السيرفر
- عدم الظهور مرة أخرى حتى بعد إعادة التثبيت

#### 3.7.2 المحتوى
- رسالة ترحيبية مختصرة
- شرح مبسط لكيفية استخدام التطبيق
- زر إغلاق

### 3.8 مربع التعليمات

#### 3.8.1 شروط الظهور
- يظهر مرة واحدة للطالب الجديد
- حفظ حالة الظهور في حساب الطالب في السيرفر
- عدم الظهور مرة أخرى حتى بعد إعادة التثبيت

#### 3.8.2 المحتوى
- نصائح سريعة لاستخدام التطبيق
- شرح الأيقونات الرئيسية
- زر إغلاق

### 3.9 شاشة الخطأ عند الفتح

#### 3.9.1 معالجة الأخطاء
- إخفاء رسالة الكاش عند الفتح من المتصفح
- عرض رسالة خطأ مختصرة فقط عند الضرورة
- الرسالة لا تمنع فتح التطبيق في وضع الأوفلاين
- الصيغة: «تحميل البيانات المحفوظة...»

#### 3.9.2 السلوك
- عرض البيانات المحلية فوراً
- محاولة الاتصال بالسيرفر في الخلفية
- عدم عرض أي رسالة خطأ متعلقة بالشبكة

---

## 4. نظام الأداء والعمل بدون إنترنت

### 4.1 كاش الصور

#### 4.1.1 حجم الكاش
- كاش دائم بحجم 200MB
- نظام حذف الأقدم (LRU) عند امتلاء الكاش

#### 4.1.2 ضغط الصور
- ضغط الصور المصغرة تلقائياً إلى 300x300 قبل التخزين
- صور داخل الدرس لا تتأثر بالضغط

#### 4.1.3 آلية العرض
- عرض فوري من الجهاز
- تحديث من السيرفر في الخلفية (عند وجود إنترنت)
- الاحتفاظ بآخر صورة عند عدم الإنترنت
- يمنع اختفاء الصورة أو ظهور أيقونة خطأ

### 4.2 كاش الدروس

#### 4.2.1 حجم الكاش
- 100MB لكاش الدروس
- نظام حذف الأقدم (LRU) عند امتلاء الكاش

#### 4.2.2 صلاحية البيانات
- صلاحية البيانات 5 دقائق
- بعد 5 دقائق، تحديث تلقائي عند توفر الإنترنت

#### 4.2.3 إدارة البيانات
- حذف الدروس المحذوفة تلقائياً
- إضافة الدروس الجديدة تلقائياً
- المزامنة مع حساب الطالب في السيرفر

### 4.3 التنقل الفوري بين الصفحات

#### 4.3.1 آلية التنقل
- الضغط على أيقونة ينقل فوراً للصفحة دون انتظار السيرفر
- داخل الصفحة عرض آخر بيانات من الجهاز فوراً
- تحديث البيانات في الخلفية بهدوء (عند وجود إنترنت)

#### 4.3.2 الصفحات المشمولة
- Dashboard
- Lessons
- Quizzes
- Notes
- Statistics

### 4.4 مؤشر التحديث الصامت

#### 4.4.1 الشكل
- نقطة خضراء صغيرة
- تظهر لمدة ثانيتين فقط
- تظهر عند التحديث في الخلفية

#### 4.4.2 الموقع
- في أعلى الشاشة
- بجانب أيقونة التحديث اليدوي

### 4.5 التحديث التلقائي للمحتوى

#### 4.5.1 آلية التحديث
- عند فتح التطبيق ووجود إنترنت:
  + التحقق من وجود محتوى جديد في السيرفر
  + إذا وُجد محتوى جديد:
    * عرض تنبيه صغير: «يوجد محتوى جديد»
    * تحديث المحتوى تلقائياً في الخلفية
    * عرض مؤشر التحديث الصامت

#### 4.5.2 التنبيه
- رسالة مختصرة في أعلى الشاشة
- تختفي تلقائياً بعد 3 ثوانٍ
- لا تعيق استخدام التطبيق

### 4.6 نظام التشفير

#### 4.6.1 آلية التشفير
- تشفير جميع بيانات الكاش بمفتاح مستمد من معرف الحساب
- التشفير لا يمنع فتح التطبيق في وضع الأوفلاين

#### 4.6.2 البيانات المشفرة
- معرف الحساب في localStorage
- الدروس المخزنة
- الصور المخزنة
- الملاحظات المحلية
- الأسئلة المحلية

---

## 5. نظام دخول المدير

### 5.1 آلية الدخول
- يستمر المدير باستخدام Supabase Auth
- رمز سري افتراضي: 732742752
- إمكانية تغيير الرمز السري

### 5.2 الصلاحيات
- الوصول الكامل لجميع وظائف الإدارة
- إدارة الأكواد والصفوف والمواد
- إعادة تعيين أكواد التفعيل
- حذف حسابات الطلاب
- جميع وظائف المدير الحالية تبقى كما هي

### 5.3 موقع رابط الدخول
- أسفل عبارة: © 2026 الوسيلة الذكية | جميع العلامات التجارية محفوظة
- في صفحة الدخول الرئيسية

---

## 6. قواعد العمل والمنطق

### 6.1 التحقق من الأكواد

#### 6.1.1 كود 6 خانات (التفعيل الأول)
- التحقق في جدول access_codes
- التأكد من صلاحية الكود
- التأكد من عدم استخدام الكود مسبقاً
- إنشاء حساب جديد في جدول student_accounts
- توليد كلمة مرور تلقائية
- تمييز الكود كمستخدم

#### 6.1.2 كود 7 خانات (تفعيل صف)
- التحقق في جدول class_activation_codes
- التأكد من ارتباط الكود بصف محدد
- التأكد من صلاحية الكود
- إضافة الصف لحساب الطالب في السيرفر

### 6.2 إدارة الصلاحيات

#### 6.2.1 الوصول الكامل
- عرض جميع الصفوف الدراسية
- عرض جميع المواد الدراسية
- الوصول لجميع الدروس والاختبارات

#### 6.2.2 الوصول المحدود
- عرض الصفوف المفعّلة فقط
- عرض المواد المرتبطة بالصفوف المفعّلة
- الوصول للدروس والاختبارات الخاصة بالصفوف المفعّلة

### 6.3 تسجيل الخروج
- حذف معرف الحساب من localStorage
- الاحتفاظ بالبيانات المشفرة في الكاش
- إعادة التوجيه للصفحة الرئيسية

### 6.4 المزامنة مع السيرفر

#### 6.4.1 المزامنة التلقائية
- عند فتح التطبيق ووجود إنترنت:
  + التحقق من صلاحية الحساب
  + تحديث تاريخ آخر دخول
  + مزامنة الملاحظات والأسئلة المحفوظة
  + مزامنة نتائج الاختبارات
  + مزامنة بيانات الإحصائيات

#### 6.4.2 المزامنة اليدوية
- الضغط على أيقونة التحديث:
  + جلب المحتوى الجديد من السيرفر
  + مزامنة جميع البيانات
  + تحديث الكاش المحلي

### 6.5 معالجة الأخطاء

#### 6.5.1 أخطاء الشبكة
- معالجة جميع أخطاء الشبكة في الخلفية
- عدم عرض رسائل خطأ مزعجة للمستخدم
- الاستمرار بالبيانات المخزنة
- محاولة إعادة الاتصال تلقائياً

#### 6.5.2 أخطاء التخزين
- معالجة أخطاء التخزين في الخلفية
- محاولة إعادة التخزين تلقائياً
- عدم عرض رسائل خطأ للمستخدم

#### 6.5.3 أخطاء المزامنة
- الاحتفاظ بالبيانات المحلية
- محاولة المزامنة في الفتح التالي
- عدم فقدان أي بيانات

### 6.6 قواعد الثبات
- البيانات التي تم تنزيلها لا تختفي أبداً
- التطبيق يعمل بآخر بيانات مخزنة حتى في وضع الأوفلاين
- عدم حذف أي بيانات إلا عند:
  + تسجيل الخروج اليدوي
  + إعادة تعيين الكود من المدير
  + حذف الحساب من المدير

---

## 7. التغييرات التقنية المطلوبة

### 7.1 قاعدة البيانات

#### 7.1.1 جدول جديد: student_accounts
- id: معرف فريد
- activation_code: الكود المستخدم للتفعيل
- password_hash: كلمة المرور المشفرة
- activated_classes: قائمة الصفوف المفعّلة (JSON)
- last_login: تاريخ آخر دخول
- install_shown: حالة عرض رسالة التثبيت
- tutorial_shown: حالة عرض التعليمات
- saved_notes: الملاحظات المحفوظة (JSON)
- saved_questions: الأسئلة المحفوظة (JSON)
- statistics_data: بيانات الإحصائيات (JSON)
- created_at: تاريخ الإنشاء

#### 7.1.2 تعديل جدول access_codes
- إضافة حقل: is_used (boolean)
- إضافة حقل: used_at (timestamp)
- إضافة حقل: student_account_id (foreign key)

#### 7.1.3 الجداول الموجودة
- class_activation_codes: تبقى كما هي
- جميع الجداول الأخرى: تبقى كما هي

### 7.2 الملفات المطلوب حذفها
- src/pages/StudentRegister.tsx
- src/pages/StudentLogin.tsx (القديم)
- src/pages/student/Account.tsx (استبداله بصفحة تفعيل كود جديد)

### 7.3 الملفات المطلوب إنشاؤها
- src/contexts/StudentAccountContext.tsx
- src/pages/StudentAccountLogin.tsx
- src/pages/student/ActivateNewCode.tsx
- src/utils/accountManager.ts
- src/utils/syncManager.ts
- src/utils/cacheManager.ts
- src/utils/imageCache.ts
- src/utils/dataEncryption.ts
- src/utils/dataMigration.ts

### 7.4 الملفات المطلوب تعديلها
- src/components/RouteGuard.tsx
- src/pages/student/Dashboard.tsx
- src/pages/student/Lessons.tsx
- src/pages/student/Quizzes.tsx
- src/pages/student/Notes.tsx
- src/pages/student/Statistics.tsx
- جميع الصفحات التي تعرض الصور
- جميع الصفحات التي تعتمد على نظام الحسابات القديم

### 7.5 التعديلات على الواجهة
- نقل رابط دخول المدير ليكون أسفل عبارة: © 2026 الوسيلة الذكية | جميع العلامات التجارية محفوظة
- إضافة أيقونة تحديث يدوي في أعلى القائمة الرئيسية وقائمة الدروس
- إضافة مؤشر التحديث الصامت (النقطة الخضراء)
- توحيد حجم العرض في جميع الصفحات
- إزالة جميع رسائل الخطأ المتعلقة بالشبكة
- تحسين رسالة الخطأ عند الفتح

---

## 8. الحالات الاستثنائية والحدود

| الحالة | السلوك المتوقع |
|--------|----------------|
| إدخال كود غير صحيح | عرض رسالة خطأ واضحة |
| إدخال كود منتهي الصلاحية | عرض رسالة انتهاء الصلاحية |
| إدخال كود مستخدم مسبقاً | عرض رسالة: «هذا الكود مستخدم مسبقاً» |
| إدخال كلمة مرور خاطئة | عرض رسالة: «كلمة المرور غير صحيحة» |
| محاولة الوصول لصف غير مفعّل | عدم عرض الصف في القائمة |
| فقدان الاتصال بالإنترنت | العمل من التخزين المحلي دون رسائل خطأ |
| إعادة تعيين الكود من المدير | مسح البيانات والعودة لشاشة الدخول |
| حذف التطبيق وإعادة التثبيت | الدخول بكلمة المرور واستعادة جميع البيانات |
| تغيير المتصفح | الدخول بكلمة المرور دون الحاجة للكود |
| تغيير الجهاز | الدخول بكلمة المرور دون الحاجة للكود |
| امتلاء كاش الصور | حذف الصور الأقدم تلقائياً |
| امتلاء كاش الدروس | حذف الدروس الأقدم تلقائياً |
| فشل التشفير | الرجوع للبيانات غير المشفرة |
| فشل ترحيل البيانات | الاحتفاظ بالبيانات المحلية وإعادة المحاولة |
| خطأ في الشبكة | معالجة في الخلفية دون رسائل |
| خطأ في التخزين | معالجة في الخلفية دون رسائل |
| فشل المزامنة | الاحتفاظ بالبيانات المحلية وإعادة المحاولة |
| فتح التطبيق من رابط | الدخول بكلمة المرور إذا لم يكن مسجلاً |
| فتح التطبيق من متصفح آخر | الدخول بكلمة المرور إذا لم يكن مسجلاً |

---

## 9. معايير القبول

1. إدخال كود 6 خانات للمرة الأولى ينشئ حساباً ويعرض كلمة المرور
2. إدخال كلمة المرور يدخل للتطبيق من أي جهاز أو متصفح
3. حذف التطبيق وإعادة التثبيت: الدخول بكلمة المرور يستعيد جميع البيانات
4. تغيير المتصفح: الدخول بكلمة المرور دون الحاجة للكود
5. إعادة تعيين الكود من المدير: طرد الطالب عند فتح التطبيق مع إنترنت
6. الملاحظات المحفوظة تظهر بعد إعادة التثبيت
7. الأسئلة المحفوظة تظهر بعد إعادة التثبيت
8. آخر تاريخ دخول يظهر في Dashboard
9. الإحصائيات تُجلب من السيرفر ولا تعرض رسالة الكود
10. رسالة التثبيت تظهر مرة واحدة فقط
11. مربع التعليمات يظهر مرة واحدة فقط
12. أيقونة التحديث اليدوي تعمل في القائمة الرئيسية وقائمة الدروس
13. مؤشر التحديث الصامت (النقطة الخضراء) يظهر عند التحديث في الخلفية
14. رابط دخول المدير يظهر أسفل عبارة: © 2026 الوسيلة الذكية | جميع العلامات التجارية محفوظة
15. حجم العرض موحد في جميع الصفحات دون تغيير بناءً على حالة الاتصال
16. رسالة الخطأ عند الفتح مختصرة ولا تمنع استخدام التطبيق
17. التطبيق يعمل بدون إنترنت بعد الدخول الأولي
18. التحديث التلقائي للمحتوى عند وجود محتوى جديد
19. ترحيل المستخدمين القدامى يتم تلقائياً عند أول فتح بعد التحديث
20. جميع البيانات المخزنة مشفرة
21. كاش الصور لا يتجاوز 200MB
22. كاش الدروس لا يتجاوز 100MB
23. لا تظهر أي رسالة خطأ متعلقة بالشبكة
24. التنقل بين الصفحات فوري دون انتظار

---

## 10. ما لا يتم تنفيذه في هذه المرحلة

- إعادة تصميم واجهة المستخدم بالكامل
- تعديل جداول قاعدة البيانات الحالية (عدا إضافة جدول student_accounts وتعديل access_codes)
- إضافة ميزات جديدة غير مذكورة
- تغيير آلية عمل الذكاء الاصطناعي
- تعديل نظام الأكواد الحالي (6 و7 خانات)
- إضافة أنواع أكواد جديدة
- تعديل شاشة الملخص والأسئلة
- تعديل لوحة الإدارة (عدا إضافة وظيفة إعادة تعيين الكود)
- تغيير الاستضافة
- تعديل أي API (عدا إضافة APIs لإدارة الحسابات)
- إضافة ميزة السحب للتحديث
- استخدام مؤشر التحميل الدائري
- تغيير نظام دخول المدير