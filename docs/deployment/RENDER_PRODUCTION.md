# Render deployment note — superseded

Render is no longer treated as the final production hosting target.

As of 2026-09-08, Render is used only as a temporary **development/test environment** while the product is being built. Final production hosting is expected to move to a VPS or Railway after requirements stabilize.

Canonical current document:

`docs/deployment/RENDER_DEVELOPMENT.md`

Do not use this file as deployment authority.
