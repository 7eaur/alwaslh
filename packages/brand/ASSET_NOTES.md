# Brand asset notes

Canonical vector source: `assets/logo/logo-mark.svg`.

Raster PWA icons are derivatives of that mark. If the logo geometry changes, regenerate all raster assets rather than editing them manually.

The visual board under `reference/brand-direction-board.svg` is a presentation reference only. Product code should import canonical assets and tokens, not sample-board coordinates.
